name: Get photos from instagram
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Get photos from instagram
        run: curl -x GET -d {} https://vetslon.spb.ru/api/getInstagramPhotos?username=ilyapasyuk
